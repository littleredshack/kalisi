import { Injectable } from '@angular/core';

/**
 * Dynamic layout service - stub only
 * ViewGraph handles all mutations directly, no separate layout service needed
 */
@Injectable({
  providedIn: 'root'
})
export class DynamicLayoutService {
  // Stubbed - all logic moved to ViewGraph direct mutation
}
